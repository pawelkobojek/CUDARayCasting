//#include "RayCastingCPU.h"
/*
int shift = 0;

void rayCastingCPU(uint32_t* data, int width, int height)
{
	const float div = WIDTH / 255.0f;
	for (int x = 0; x < WIDTH; x++)
		for (int y = 0; y < HEIGHT; y++)
		{
			Uint32 color = (Uint32)(x / div);
			data[PIXEL((x + shift) % WIDTH, y)] = COLOR(color, color, color, 0);
		}
	shift += 10;
	shift %= WIDTH;
}*/